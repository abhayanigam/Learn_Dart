// Constant Keyword:
// Only constant variable can be used to compute a compiler time constant.
// Compiler-time constant whose values will be determine at compile time.

void main(){
  const pi = 3.14;
  const area = pi*12*12;

  print("The are is : $area");
}